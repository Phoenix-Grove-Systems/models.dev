# Cache key (chat): strip prompt_cache_key — OpenAI-compatible router over private LLM; no cache params or cached-token fields documented.
# Cache headers: none
# Cache policy: no documented cache — hits depend on downstream route; replay stable prefixes, treat as uncached.
# Docs: https://docs.zeldoc.ai
name = "Zeldoc"
env = ["ZELDOC_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://api.zeldoc.ai/v1"
doc = "https://docs.zeldoc.ai"
