# Cache key (chat): strip prompt_cache_key — OpenAI-shape chat with no cache params; flat per-token pricing has no cached rate.
# Cache headers: none
# Cache policy: plain passthrough with no documented cache behavior; no cache discounts to target.
# Docs: https://klokintegration.se/docs/ai-api
name = "klokintegration.se"
npm = "@ai-sdk/openai-compatible"
env = ["KLOKINTEGRATION_API_KEY"]
api = "https://api-gw.klok.ipaas.se/proxy/kloker-key/v1"
doc = "https://klokintegration.se/docs/ai-api"
