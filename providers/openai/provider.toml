# Prompt cache (chat): SUPPORTS — automatic prefix caching by default, plus explicit breakpoints, keys, and TTL controls.
# Docs: https://platform.openai.com/docs/guides/prompt-caching
name = "OpenAI"
env = ["OPENAI_API_KEY"]
npm = "@ai-sdk/openai"
doc = "https://platform.openai.com/docs/models"
