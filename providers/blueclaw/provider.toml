# Cache key (chat): send prompt_cache_key — docs describe a plain OpenAI-compatible routing gateway, silent on cache, with no strict schema published; the SDK-passed key forwards to operators.
# Cache headers: none.
# Cache policy: no key-based control; keep stable shared prefixes so operator-side reuse can apply.
# Docs: https://blueclaw.network/how-it-works.html
name = "Blue Claw"
npm = "@ai-sdk/openai-compatible"
api = "https://openai.blueclaw.network/v1"
env = ["BLUECLAW_API_KEY"]
doc = "https://blueclaw.network"
