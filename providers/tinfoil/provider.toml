# Prompt cache (chat): NATIVE_OTHER — automatic prefix cache scoped by SDK-managed `user_cache_secret`, not by chat cache params.
# Docs: https://docs.tinfoil.sh/sdk/prompt-caching
name = "Tinfoil"
npm = "@ai-sdk/openai-compatible"
api = "https://inference.tinfoil.sh/v1"
env = ["TINFOIL_API_KEY"]
doc = "https://docs.tinfoil.sh"
