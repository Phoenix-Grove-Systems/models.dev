# Cache key (chat): strip prompt_cache_key — OpenAI-compatibility guide documents no cache params; unsupported fields are ignored.
# Cache headers: none
# Cache policy: none documented — no native cache control.
# Docs: https://platform.scx.ai/docs/guides/openai-compatibility
name = "SCX.ai"
env = ["SCX_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://api.scx.ai/v1"
doc = "https://platform.scx.ai/docs"
