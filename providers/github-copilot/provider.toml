# Cache key (chat): strip prompt_cache_key — private POST /chat/completions with authenticated GET /models schema documents no key; public billing docs show cached-input/cache-write rates only; SDK is generic openai-compatible.
# Cache headers: none
# Cache policy: automatic prefix caching; keep stable prefix on same model/conversation with no client key; verify via cached-input billing.
# Docs: https://docs.github.com/en/copilot/reference/copilot-billing/models-and-pricing
name = "GitHub Copilot"
# Reasoning HTTP format (accessed 2026-06-25): POST /chat/completions.
# Supported controls and values come from authenticated GET /models metadata
# and can change by account/model; GitHub's public model comparison does not
# document this private request schema or static mappings.
# https://api.githubcopilot.com/models
# https://docs.github.com/en/copilot/reference/ai-models/model-comparison
env = ["GITHUB_TOKEN"]
npm = "@ai-sdk/openai-compatible"
doc = "https://docs.github.com/en/copilot"
api= "https://api.githubcopilot.com"
