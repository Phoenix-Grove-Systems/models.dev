# Prompt cache (chat): TOLERATES — automatic provider-native prefix cache with no router setting; `prompt_cache_retention` returns 501.
# Docs: https://trustedrouter.com/docs/prompt-caching
name = "TrustedRouter"
env = ["TRUSTEDROUTER_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://api.trustedrouter.com/v1"
doc = "https://trustedrouter.com/docs"
