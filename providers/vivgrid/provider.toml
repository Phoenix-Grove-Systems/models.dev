# Cache key (chat): strip prompt_cache_key — chat completions schema omits cache fields (model/messages/tools/reasoning_effort only); no key on the chat surface.
# Cache headers: none
# Cache policy: implicit upstream cache with Cached Token billing; keep prefixes stable, no client key on chat.
# Docs: https://docs.vivgrid.com/models
name = "Vivgrid"
env = ["VIVGRID_API_KEY"]
api = "https://api.vivgrid.com/v1"
npm = "@ai-sdk/openai"
doc = "https://docs.vivgrid.com/models"
