# Cache key (chat): strip prompt_cache_key — experimental chat schema documents only model+messages (plus image_url); no cache params.
# Cache headers: none
# Cache policy: no documented prefix-cache policy or cached-token reporting; no cache discount
# Docs: https://docs.hetzner.com/general/company-and-policy/experiments/inference/
name = "Hetzner"
env = ["HETZNER_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://inference.hetzner.com/api/v1"
doc = "https://experiments.hetzner.com/docs/inference"
