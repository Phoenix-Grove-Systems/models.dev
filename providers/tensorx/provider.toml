# Prompt cache (chat): REJECTS — unknown cache fields fail with 400; caching is automatic best-effort KV prefix reuse.
# Docs: https://docs.tensorx.ai/api-reference/prompt-caching
name = "TensorX"
env = ["TENSORX_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://api.tensorx.ai/v1"
doc = "https://docs.tensorx.ai/"
