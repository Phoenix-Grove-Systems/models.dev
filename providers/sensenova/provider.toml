# Cache key (chat): strip prompt_cache_key — OpenAI-compatible endpoint docs list no cache request params.
# Cache headers: none
# Cache policy: none documented — no native cache control.
# Docs: https://platform.sensenova.cn/docs
name = "SenseNova (China)"
npm = "@ai-sdk/openai-compatible"
env = ["SENSENOVA_API_KEY"]
api = "https://token.sensenova.cn/v1"
doc = "https://platform.sensenova.cn/docs"
