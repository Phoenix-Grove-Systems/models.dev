# Cache key (chat): strip prompt_cache_key — quickstart documents only model+messages, no cache params; relay to 50+ upstreams with mixed strictness.
# Cache headers: none
# Cache policy: no client cache control; automatic upstream passthrough only, keep prefixes stable.
# Docs: https://unorouter.com/en/docs/platform/quickstart
name = "UnoRouter"
env = ["UNOROUTER_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://api.unorouter.com/v1"
doc = "https://unorouter.com/models"
