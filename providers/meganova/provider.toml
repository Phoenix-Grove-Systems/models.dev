# Cache key (chat): strip prompt_cache_key — chat body is messages/model/max_tokens/temperature/top_p/stream; no prompt_cache_key, usage prompt_tokens_details null.
# Cache headers: none
# Cache policy: no chat prefix cache; per-model cached-input prices reflect upstream.
# Docs: https://docs.meganova.ai/inference-models/text-generation.md
# POST /v1/chat/completions publishes messages, model, max_tokens, temperature,
# top_p, and stream; no toggle, effort, or reasoning-token budget is listed.
# https://docs.meganova.ai/inference-models/text-generation.md (accessed 2026-06-25)
name = "Meganova"
env = ["MEGANOVA_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://api.meganova.ai/v1"
doc = "https://docs.meganova.ai"
