# Cache key (chat): strip prompt_cache_key — cache guide documents implicit context cache with no key param.
# Cache headers: none
# Cache policy: implicit context cache on repeated system prompts/prefixes; usage.prompt_tokens_details.cached_tokens billed at discount.
# Docs: https://docs.bigmodel.cn/cn/guide/capabilities/cache
# Chat reasoning: POST https://open.bigmodel.cn/api/paas/v4/chat/completions with
# `thinking.type = "enabled" | "disabled"` (default: enabled).
# https://docs.bigmodel.cn/cn/guide/capabilities/thinking (accessed 2026-06-25)
name = "Zhipu AI"
env = ["ZHIPU_API_KEY"]
npm = "@ai-sdk/openai-compatible"
doc = "https://docs.z.ai/guides/overview/pricing"
api = "https://open.bigmodel.cn/api/paas/v4"