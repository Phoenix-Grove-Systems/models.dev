# Prompt cache (chat): TOLERATES — no documented cache fields on chat; cache-read billing applied server-side.
# Docs: https://neosmith.ai/docs
name = "NeoSmith"

env = ["NEOSMITH_API_KEY"]

npm = "@ai-sdk/openai"

api = "https://router.neosmith.ai/v1"

doc = "https://neosmith.ai/docs"
