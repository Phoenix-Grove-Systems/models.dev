# Prompt cache (chat): SUPPORTS — automatic prefix cache; usage.cache_read_tokens on chat plus published cache_read rate.
# Docs: https://api.koscompute.com/docs/
name = "Kosmik Compute"
env = ["KOSMIK_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://api.koscompute.com/v1"
doc = "https://api.koscompute.com/docs/"
